# Звіт до роботи
## Тема: Розробка програми з використанням ООП
### Мета роботи: Навчитись створювати класи в Python, використовувати перечислення (Enum), властивості (@property), та застосовувати основні принципи об'єктно-орієнтованого програмування для розробки програми яка імітує роботу бібліотеки

---
### Виконання роботи
* Результати виконання завдання:
    1. Розробили програму для імітації роботи бібліотеки з використанням ООП
    2. Створили клас `BookState` (Enum) для опису станів книги:
        - AVAILABLE (Доступна)
        - BORROWED (Позичена)
        - RESERVED (Зарезервована)
        - LOST (Втрачена)
    3. Створили клас `Book` з наступними характеристиками:
        - Атрибути: `title` (назва), `author` (автор), `year_published` (рік публікації), `state` (стан)
        - Властивість `@property description` для опису книги
        - Метод `__str__` для зручного представлення об'єкта
    4. Створили клас `Student` з можливостями:
        - Атрибути: `first_name`, `last_name`, `student_id` (унікальний номер), `borrowed_books` (список позичених книг)
        - Метод `borrow_book()` для позики книги з бібліотеки
        - Метод `return_book()` для повернення книги до бібліотеки
    5. Навчились використовувати перечислення (Enum) для керування станами об'єктів
    6. Отримали практичні навички роботи з властивостями класів через декоратор @property
    7. Програма успішно демонструє взаємодію між об'єктами класів Book та Student

* Код класу Book:
```python
from enum import Enum

class BookState(Enum):
    AVAILABLE = "Доступна"
    BORROWED = "Позичена"
    RESERVED = "Зарезервована"
    LOST = "Втрачена"

class Book:
    def __init__(self, title, author, year_published):
        self.title = title
        self.author = author
        self.year_published = year_published
        self.state = BookState.AVAILABLE

    @property
    def description(self):
        return f"Назва: '{self.title}',\n Автор: {self.author},\n Опубліковано в {self.year_published}."
    
    def __str__(self):
        return f"Книга: '{self.title}'"
```

* Код класу Student:
```python
from book import Book, BookState

class Student:
    def __init__(self, first_name, last_name):
        self.first_name = first_name
        self.last_name = last_name
        self.student_id = id(self)  # Унікальний номер студентського квитка
        self.borrowed_books: list[Book] = []

    def borrow_book(self, book: Book):
        if book.state != BookState.AVAILABLE:
            print(f"Книга '{book.title}' недоступна для позики.")
            return
        book.state = BookState.BORROWED
        self.borrowed_books.append(book)
        print(f"{self.first_name} взяв книгу '{book.title}'.")
    
    def return_book(self):
        if not self.borrowed_books:
            print(f"{self.first_name} не має книг для повернення.")
            return
        book = self.borrowed_books.pop()
        book.state = BookState.AVAILABLE
        print(f"{self.first_name} повернув книгу '{book.title}'.")
```

* Приклад використання програми:
```python
# Створення книг
book_shelf = []
for name in ["Посібник Python", "Вивчення JavaScript", "Основи C++"]:
    book = Book(title=name, author="Не вказано", year_published=2025)
    book_shelf.append(book)

# Створення студента
student = Student("Богдан", "Bobas")

# Позичення книг
student.borrow_book(book_shelf[0])  # Богдан взяв книгу 'Посібник Python'
student.borrow_book(book_shelf[1])  # Богдан взяв книгу 'Вивчення JavaScript'

# Повернення книги
student.return_book()  # Богдан повернув книгу 'Вивчення JavaScript'
```

* Результат виконання програми:
```text
Богдан взяв книгу 'Посібник Python'.
Богдан взяв книгу 'Вивчення JavaScript'.
Богдан повернув книгу 'Вивчення JavaScript'.
```

---
### Висновок:
> у висновку потрібно відповісти на запитання:

- :question: **Що зроблено в роботі:**
  - Створено програму для імітації роботи бібліотеки з використанням об'єктно-орієнтованого програмування
  - Розроблено три основні класи: BookState (Enum), Book та Student
  - Реалізовано функціонал позичення та повернення книг студентами
  - Використано перечислення для керування станами книг
  - Застосовано декоратор @property для створення властивостей класу

- :question: **Чи досягнуто мети роботи:**
  - Так, мета роботи повністю досягнута. Ми успішно навчились створювати класи, використовувати Enum, property та застосовувати принципи ООП на практиці

- :question: **Які нові знання отримано:**
  - Навчились працювати з перечисленнями (Enum) в Python
  - Отримали розуміння про використання декоратора @property
  - Навчились проектувати взаємодію між класами
  - Засвоїли принципи інкапсуляції та управління станом об'єктів
  - Практично застосували концепції ООП для моделювання реальних сценаріїв

- :question: **Чи вдалось відповісти на всі питання задані в ході роботи:**
  - Так, всі питання щодо створення класів, використання перечислень та реалізації методів були успішно вирішені

- :question: **Чи вдалося виконати всі завдання:**
  - Так, всі завдання виконано повністю: створено класи Book, BookState та Student з необхідним функціоналом

- :question: **Чи виникли складності у виконанні завдання:**
  - Складнощів не виникло, завдання було зрозумілим та логічно структурованим. Використання Enum та @property було цікавим досвідом

- :question: **Чи подобається такий формат здачі роботи (Feedback):**
  - Так, формат здачі роботи через GitHub дуже зручний, дозволяє систематизувати знання та зберігати історію змін

- :question: **Побажання для покращення (Suggestions):**
  - Можна додати додаткові завдання на розширення функціоналу бібліотеки (наприклад, клас Library з методами пошуку книг, обмеження кількості книг на руках у студента)
  - Було б корисно додати завдання на обробку винятків для більш надійної роботи програми

---
